# Verification notes

- 2026-08-23: The local preview loaded the Arabic login screen after the profile and mobile-header changes. The browser subsystem then entered a crash-loop state before authenticated visual capture could proceed, so authenticated pages still require follow-up visual verification after recovery.
